# Uncomment the next line to define a global platform for your project
  platform :ios, '13.2'

target 'RailsTask' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'SwiftyJSON'
  pod 'GraphQLicious', '~> 0.8'

  target 'RailsTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
